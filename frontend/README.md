# frontend

> Placeholder (P0 skeleton). React + Tailwind + vtk.js UI — viewer, retrieval, history, export, task status. Lands in **P1+** (visual direction chosen in P14).

No implementation exists yet — see the Master Plan phase that introduces this component.
